node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# Debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Env
.env*.local

# Firebase — google-services.json is safe to commit (public identifiers only),
# but never commit the FCM service account private key if it's ever downloaded
# into this folder. It should go straight into `eas credentials`, not git.
*service-account*.json
firebase-adminsdk*.json

# TypeScript
*.tsbuildinfo
